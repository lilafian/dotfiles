# Dotfiles
These are ***NOT STABLE*** and will change between DEs and WMs by default!
These are also not up-to-date and files (like `.zshrc`) are out of sync! (use `.zshrc.bak`)

Use `sh install.sh [x/wayland]` to install the dots
